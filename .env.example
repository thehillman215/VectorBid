# Optional: protects /export when set
VECTORBID_API_KEY=changeme

# Where /export writes JSON files
EXPORT_DIR=exports

# Dev server port
PORT=8000

# Landing page variant to use on the home route
NEXT_PUBLIC_LANDING=v1
